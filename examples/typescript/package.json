{
  "name": "marona-a2a-typescript-example",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "peer": "node dist/peer.js",
    "realtime": "node dist/realtime.js",
    "test": "pnpm run build && node --test test/*.test.mjs"
  },
  "dependencies": {
    "marona": "0.13.8"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  }
}

